/*
函数
 1.函数定义
   <返回值类型> <函数名>(<参数表>)
   {
       <函数执行内容>
   }
 2.返回值类型 int double等等
    其中void 代表无返回值
 3.函数结束 return
   在函数中一旦执行到return将结束函数的执行。
   对于有返回值的函数必须有return <返回值>;
   对于void的函数可以没有return，提前结束函数可用return;
 4.参数表中如果包含数组，在该参数后添加[]即可，无需说明该数组元素数量







 */